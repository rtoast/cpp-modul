#include "iter.hpp"

int		main() {
	int arr[3] = {1, 2, 3}; 
	iter(arr, 3, OMG);

    double Darr[3] = {4.7, 5.8, 6.9}; 
	iter(Darr, 3, OMG);

    char Carr[3] = {'O', 'M', 'G'}; 
	iter(Carr, 3, OMG);

    return(0);
}